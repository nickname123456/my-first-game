# PM Simulator

Игрок берет на себя роль проджект-менеджера в маленьком IT-стартапе, где до релиза осталось совсем немного времени, а команда уже живет в режиме "сейчас все починим". Нужно ходить по офису, общаться с сотрудниками, помогать им решать проблемы и принимать решения, от которых зависит судьба продукта. Игра иронично показывает типичный хаос разработки: внезапные правки от заказчика, горящие серверы, дедлайны, выгорание и код, который "почти работает".

## Основная идея

**Жанр:** 2D top-down тайм-менеджмент / стратегия с элементами симулятора офиса.

**Сеттинг:** маленький IT-стартап с офисом, рабочей зоной, кухней, переговоркой и канбан-доской.

**Роль игрока:** проджект-менеджер, который не пишет весь код сам, а помогает команде дойти до релиза.

**Цель игрока:** довести продукт до релиза. В конце игры подсчитываются баллы по тому, как игрок справлялся с задачами, дедлайнами, проблемами команды и запросами заказчика.

**Главное действие игрока:** перемещаться по офису, разговаривать с подчиненными, назначать задачи, реагировать на кризисы и выбирать, кому и как помочь.

## Как это работает

1. **Доска задач:** в офисе есть канбан-доска, на которую приходят задачи с параметрами: срочность, сложность, нужный навык и дедлайн.
2. **Команда:** у каждого сотрудника есть роль, сильные и слабые стороны, текущая загрузка и настроение.
3. **Общение:** игрок подходит к сотрудникам, узнает их проблемы и выбирает варианты помощи.
4. **События:** во время работы случаются кризисы, на которые нужно быстро реагировать.
5. **Финал:** если продукт доходит до релиза, игра считает итоговый результат и выдает оценку работы менеджера.

## Игровые механики

**Управление персонажем:** игрок перемещается по офису с помощью клавиш `WASD` или стрелок. Клавиша `E` используется для взаимодействия с объектами: канбан-доской, сотрудниками, событиями и диалогами.

**Канбан-доска и задачи:** задачи появляются на доске во время игры. У каждой задачи есть тип, сложность, дедлайн и подходящий специалист. Игрок сам выбирает задачу и назначает ее одному из сотрудников.

**Сотрудники:** в команде есть девопс, тестер, бэкендер, фронтендер и ИИ-инженер. У каждого сотрудника своя специализация, текущая загрузка и состояние. Если задача подходит сотруднику, она выполняется быстрее и с меньшим риском проблем. Если назначить задачу не тому человеку или перегрузить сотрудника, возрастает шанс ошибки.

**Проблемы сотрудников:** проблемы возникают не полностью случайно, а зависят от задач и решений игрока. Сложные задачи, просроченные дедлайны, неподходящий исполнитель и высокая нагрузка увеличивают шанс кризиса.

**Диалоги и решения:** когда у сотрудника возникает проблема, игрок может подойти к нему и открыть диалог. В диалоге нужно выбрать вариант решения: помочь лично, подключить другого сотрудника, выделить бюджет, перенести дедлайн или проигнорировать проблему. Каждый выбор влияет на ресурсы проекта.

**Кризисные события:** у событий есть таймер реакции. Если игрок успевает разобраться с проблемой, последствия можно смягчить или полностью отменить. Если не успевает, страдают качество продукта, мораль команды, бюджет или доверие заказчика.

**Прогресс до релиза:** игра длится ограниченное время, например 5 игровых минут. За это время нужно выполнить достаточное количество задач и не довести ключевые показатели до нуля. Когда таймер релиза заканчивается, проект выпускается, а игра считает итоговые баллы.

**Система очков:** финальный счет складывается из нескольких факторов: сколько задач выполнено, сколько задач закрыто вовремя, насколько высоким осталось качество продукта, сколько сохранилось бюджета, насколько команда не выгорела, какой уровень доверия заказчика остался и насколько низким оказался технический долг.

**Победа:** игрок побеждает, если релиз состоялся и основные показатели остались выше критического уровня.

**Поражение:** игра заканчивается досрочно, если бюджет, мораль команды, качество продукта или доверие заказчика падают до нуля. Также можно проиграть, если технический долг становится слишком большим и продукт перестает быть пригодным к релизу.

## Ресурсы

- **Время до релиза** - главный ограничитель, который постоянно уменьшается.
- **Бюджет** - тратится на решения проблем, переработки и срочные исправления.
- **Мораль команды** - падает от перегруза, конфликтов и плохих решений.
- **Качество продукта** - зависит от правильно выполненных задач и количества багов.
- **Технический долг** - растет, если часто выбирать быстрые, но грязные решения.
- **Доверие заказчика** - меняется в зависимости от сроков, качества и реакции на новые требования.

## Требования к проекту

### Измеримые критерии успешности

- Одна игровая сессия длится **2-3 минуты**.
- В игре есть **5 сотрудников**: девопс, тестер, бэкендер, фронтендер и ИИ-инженер.
- В пуле есть минимум **12 разных задач**, из которых за одну сессию появляется примерно **8-12 задач**.
- В игре есть минимум **5 типов кризисных событий**.
- Игрок может довести проект до релиза и получить итоговый счет.
- Игра сохраняет **лучший итоговый счет**, но не сохраняет полное состояние прохождения.
- Все основные ресурсы отображаются на HUD: время, бюджет, мораль, качество, технический долг и доверие заказчика.
- Игра стабильно работает с частотой не ниже **30 FPS** на учебном компьютере.

### Функциональные требования

1. **Главное меню**
   - В игре должен быть стартовый экран.
   - Игрок может начать игру или выйти.
   - Критерий готовности: после нажатия "Начать" открывается основной игровой экран.

2. **Управление игроком**
   - Игрок перемещается по офису с помощью `WASD` или стрелок.
   - Клавиша `E` используется для взаимодействия с объектами и сотрудниками.
   - Критерий готовности: игрок ходит по карте и не проходит сквозь препятствия.

3. **Карта офиса**
   - Офис помещается на один экран без прокрутки.
   - Размер окна: **1280x720**.
   - В первой версии графика может быть минималистичной: прямоугольники для стен, мебели, игрока и сотрудников.
   - Критерий готовности: на карте есть рабочая зона, переговорка, кухня и канбан-доска.

4. **Канбан-доска**
   - Игрок открывает канбан-доску через `E`.
   - Задачи и сотрудники выбираются мышью.
   - У задачи есть название, нужный навык, сложность, дедлайн и статус.
   - Критерий готовности: игрок может выбрать задачу и назначить ее сотруднику.

5. **Сотрудники**
   - В игре есть 5 сотрудников с разными специализациями.
   - У сотрудника есть состояние: `idle`, `working`, `problem`, `burnout`.
   - Над сотрудником отображается простой индикатор состояния.
   - Критерий готовности: после назначения задачи сотрудник переходит в состояние `working`, а после выполнения возвращается в `idle`.

6. **Кризисные события**
   - Кризисы возникают в зависимости от задачи, нагрузки, дедлайна и правильности назначения.
   - У кризиса есть таймер реакции.
   - Игрок может подойти к сотруднику и открыть диалог решения проблемы.
   - Критерий готовности: если игрок не успел решить проблему, игра применяет штраф к ресурсам.

7. **Диалоги**
   - Диалоги открываются поверх игры.
   - Варианты ответа выбираются мышью.
   - Решение игрока влияет на бюджет, мораль, качество, техдолг или доверие заказчика.
   - Критерий готовности: разные варианты ответа дают разные последствия.

8. **HUD**
   - На экране отображаются все ключевые ресурсы проекта.
   - Значения ресурсов меняются во время игры.
   - Критерий готовности: игрок видит последствия своих решений без открытия дополнительных меню.

9. **Победа, поражение и результат**
   - Победа наступает, если игрок доживает до релиза и выполняет минимум **6 задач**.
   - Поражение наступает, если бюджет, мораль, качество или доверие заказчика падают до 0.
   - Поражение также возможно, если технический долг достигает **100**.
   - После релиза показывается итоговый счет.
   - Критерий готовности: игра корректно переходит на экран результата или поражения.

10. **Лучший счет**
    - После завершения игры лучший счет сохраняется локально.
    - Формат хранения: простой файл `highscore.json` или `highscore.txt`.
    - Критерий готовности: после перезапуска игры лучший счет остается доступен в меню или на экране результата.

### Технические требования

- Язык программирования: **Python**.
- Библиотека: **pygame**.
- Минимальная частота кадров: **30 FPS**.
- Размер окна: **1280x720**.
- Карта офиса: один экран без камеры и прокрутки.
- Количество сотрудников: **5**.
- Минимальное количество шаблонов задач: **12**.
- Количество типов кризисов: минимум **5**.
- Ввод: клавиатура для движения и взаимодействия, мышь для канбана и диалогов.
- В первой версии звук не обязателен.
- В первой версии можно использовать геометрические фигуры вместо спрайтов.

### Ситуации для проверки алгоритмов и механик

- Если задачу для бэкендера назначить бэкендеру, алгоритм подбора сотрудника должен оценить это как хороший выбор.
- Если сложную задачу назначить неподходящему и перегруженному сотруднику, шанс кризиса должен стать выше.
- Если сотрудник получает задачу, он должен перейти из `idle` в `working`.
- Если у сотрудника возникла проблема, он должен перейти в `problem` и запустить таймер реакции.
- Если игрок не успел решить кризис, ресурсы проекта должны уменьшиться.
- Если игрок идет в стену или стол, коллизии должны остановить движение.
- Если время релиза закончилось, игра должна посчитать итоговый счет.
- Если итоговый счет больше сохраненного рекорда, лучший счет должен обновиться.

### Критерии готовности

Проект считается готовым, если:

- можно запустить игру из `main.py`;
- открывается главное меню;
- игрок может начать новую сессию;
- игрок может ходить по офису и взаимодействовать с объектами;
- работает канбан-доска и назначение задач;
- сотрудники выполняют задачи и меняют состояния;
- появляются кризисные события;
- работают диалоги с вариантами решений;
- ресурсы проекта изменяются и отображаются на HUD;
- игра завершается победой или поражением;
- показывается итоговый счет;
- лучший счет сохраняется между запусками;
- игра держит минимум 30 FPS.

### Ограничения

В первую версию осознанно не входят:

- полноценные сохранения прохождения;
- сложная пиксельная графика и анимации;
- звук и музыка;
- несколько уровней или большая карта с камерой;
- сетевой режим;
- сложный искусственный интеллект сотрудников;
- процедурная генерация офиса;
- продвинутая экономика стартапа;
- редактор карт или задач.

## Архитектура проекта

В игре используется архитектура **MVC**. Она подходит для проекта на pygame, потому что разделяет данные игры, игровую логику и отрисовку, но при этом не усложняет код лишними слоями.

### Почему MVC

**MVC** расшифровывается как Model-View-Controller:

- **Model** хранит состояние игры: игрока, сотрудников, задачи, ресурсы проекта, активные кризисы.
- **View** отвечает за отображение: офис, персонажи, HUD, канбан-доска, диалоги, меню и экран результатов.
- **Controller** принимает ввод игрока и меняет состояние модели: двигает персонажа, назначает задачи, обрабатывает диалоги и переключает игровые состояния.

Для этой игры MVC удобнее, чем **MVP** и **MVVM**:

- **MVP** больше подходит для приложений с большим количеством форм и экранов, где Presenter управляет View. Для pygame это добавило бы лишний слой между игровым циклом и отрисовкой.
- **MVVM** хорошо работает в UI-фреймворках с биндингами данных, но в pygame таких биндингов нет. Обновлять экран все равно нужно вручную каждый кадр.
- **MVC** проще объяснить и реализовать: данные лежат в объектах модели, игровой цикл вызывает обновление контроллеров, а затем View рисует текущее состояние.

### Игровые состояния

Игра делится на состояния, чтобы разные экраны не смешивались в одном большом файле:

- **MenuState** - главное меню, кнопки "Начать игру" и "Выход".
- **PlayState** - основной игровой процесс в офисе.
- **PauseState** - пауза.
- **GameOverState** - экран поражения.
- **ResultState** - экран релиза и финального счета.

Канбан-доска и диалоги сотрудников не являются отдельными полноценными сценами. Они работают как **overlay-экраны поверх PlayState**: игра остается на фоне, но ввод переключается на интерфейс канбана или диалога. На время выбора игровое время можно поставить на паузу, чтобы игрок спокойно принял решение.

### Ключевые компоненты

#### Game

Главный класс игры. Создает окно pygame, запускает главный цикл, хранит текущее состояние игры и вызывает методы `handle_event()`, `update()` и `draw()`.

**Ответственность:**

- инициализация pygame;
- создание окна;
- управление FPS;
- переключение игровых состояний;
- запуск основного цикла;
- завершение игры.

#### State / Scene

Состояние игры или отдельный экран. У каждого состояния есть одинаковый интерфейс: обработка событий, обновление логики и отрисовка.

**Ответственность:**

- `MenuState` обрабатывает главное меню;
- `PlayState` обновляет офис, сотрудников, задачи и события;
- `PauseState` показывает паузу;
- `GameOverState` показывает причину поражения;
- `ResultState` показывает итоговый счет.

#### Model

Модель хранит данные игры, но не занимается отрисовкой интерфейса. Сюда относятся игровые сущности и состояние проекта.

**Основные модели:**

- `Player` - позиция игрока, скорость, прямоугольник коллизии.
- `Employee` - роль, состояние, загрузка, текущая задача, прогресс, риск проблемы.
- `Task` - название, сложность, нужный навык, дедлайн, статус.
- `ProblemEvent` - тип кризиса, сотрудник, таймер реакции, последствия.
- `ProjectStats` - бюджет, мораль, качество, техдолг, доверие заказчика, время до релиза.

#### View

View отвечает за то, как текущее состояние игры отображается на экране. В pygame это можно реализовать простым способом: у игровых объектов и UI-компонентов есть метод `draw(surface)`, а `PlayState` вызывает их в правильном порядке.

**Что рисует View:**

- карту офиса;
- игрока и сотрудников;
- индикаторы состояния над сотрудниками;
- HUD с ресурсами проекта;
- канбан-доску;
- диалоговые окна;
- меню и финальные экраны.

#### Controller

Controller обрабатывает действия игрока и меняет Model. В проекте эту роль выполняют `Game`, активное состояние игры и отдельные обработчики ввода.

**Что делает Controller:**

- читает клавиши `WASD`, стрелки и `E`;
- двигает игрока;
- проверяет взаимодействие с объектами;
- назначает задачи сотрудникам;
- применяет выбранные решения в диалогах;
- запускает или закрывает overlay-экраны;
- проверяет условия победы и поражения.

### Менеджеры и системы

Чтобы `Game` и `PlayState` не превратились в слишком большие классы, часть логики выносится в менеджеры.

#### TaskManager

Хранит список задач, создает новые задачи, удаляет выполненные задачи и передает задачи на канбан-доску.

#### EmployeeManager

Хранит сотрудников, обновляет их состояние, назначает задачи и проверяет прогресс работы.

#### EventManager

Создает кризисные события, обновляет таймеры событий и применяет последствия, если игрок не успел среагировать.

#### CollisionManager

Хранит список препятствий офиса и проверяет столкновения игрока со стенами, столами и другими объектами через `pygame.Rect`.

#### ResourceManager

Загружает и хранит картинки, шрифты и звуки, чтобы не загружать одни и те же файлы много раз в разных частях проекта.

#### UIManager

Управляет HUD, канбан-доской, диалогами и активным overlay-экраном. Если открыт канбан или диалог, UIManager передает ввод в этот экран.

### Кто за что отвечает

| Компонент | Хранит данные | Меняет состояние | Рисует |
| --- | --- | --- | --- |
| `Game` | текущее состояние игры, окно | переключает состояния, запускает цикл | вызывает `draw()` текущего состояния |
| `PlayState` | ссылки на менеджеры и игровые объекты | обновляет игровой процесс | рисует офис, сущности и UI |
| `Player` | позиция, скорость, rect | меняет позицию после ввода | рисует игрока |
| `Employee` | роль, задача, состояние, прогресс | обновляет прогресс и состояние | рисует сотрудника и индикатор |
| `TaskManager` | список задач | создает и завершает задачи | не рисует напрямую |
| `EmployeeManager` | список сотрудников | назначает задачи, обновляет сотрудников | вызывает отрисовку сотрудников |
| `EventManager` | активные события | создает события и применяет последствия | передает данные для UI |
| `ProjectStats` | ресурсы проекта | изменяется через игровые события | отображается через HUD |
| `UIManager` | активный overlay | обрабатывает UI-ввод | рисует HUD, канбан и диалоги |
| `ResourceManager` | assets | не меняет игровую логику | отдает ресурсы для отрисовки |

### Структура файлов

```text
my-first-game/
├── main.py
├── game.py
├── settings.py
├── states/
│   ├── menu_state.py
│   ├── play_state.py
│   ├── pause_state.py
│   ├── game_over_state.py
│   └── result_state.py
├── entities/
│   ├── player.py
│   ├── employee.py
│   ├── task.py
│   └── problem_event.py
├── managers/
│   ├── task_manager.py
│   ├── employee_manager.py
│   ├── event_manager.py
│   ├── collision_manager.py
│   ├── resource_manager.py
│   └── ui_manager.py
├── ui/
│   ├── hud.py
│   ├── kanban.py
│   ├── dialog.py
│   └── button.py
└── assets/
    ├── images/
    ├── fonts/
    └── sounds/
```

### Почему архитектура удобна для этой игры

В PM Simulator много объектов, которые постоянно меняют состояние: сотрудники работают, задачи выполняются, ресурсы уменьшаются, кризисы появляются по таймеру. MVC позволяет не смешивать эти части:

- баланс и правила игры находятся в Model и менеджерах;
- ввод игрока и решения обрабатываются Controller-слоем;
- отрисовка офиса и интерфейса остается во View;
- новые экраны можно добавлять через отдельные State-классы;
- новые механики можно добавлять через менеджеры, не переписывая весь игровой цикл.

Такая архитектура достаточно простая для учебного pygame-проекта, но при этом выглядит профессионально и масштабируется, если в игру добавить больше задач, событий, сотрудников и UI-экранов.

## Алгоритмы

В проекте используются алгоритмы средней сложности: они не требуют сложной математики, но показывают работу с состояниями, списками объектов, вероятностями, игровым циклом и коллизиями.

### 1. Алгоритм подбора подходящего сотрудника для задачи

**Зачем в проекте:** помогает игроку понять, кому лучше назначить задачу. Алгоритм можно использовать для подсказки на канбан-доске: игра показывает самого подходящего сотрудника, но окончательное решение остается за игроком.

**Как реализован в проекте:** логика находится в `ui/kanban.py` или `entities/task.py`. При открытии канбан-доски алгоритм проходит по списку сотрудников и считает рейтинг каждого сотрудника для выбранной задачи. Результат используется в интерфейсе: лучший сотрудник подсвечивается или получает пометку "рекомендуется".

**Входные данные:** выбранная задача, список сотрудников, специализация сотрудника, текущая загрузка, состояние сотрудника, сложность задачи и дедлайн.

**Выходные данные:** сотрудник с максимальным рейтингом или отсортированный список сотрудников по полезности.

**Сложность:** `O(n)` по времени, где `n` - количество сотрудников. По памяти `O(1)`, если хранить только лучший вариант, или `O(n)`, если возвращать отсортированный список. В худшем случае проверяются все сотрудники.

**Псевдокод:**

```python
def find_best_employee(task, employees):
    best_employee = None
    best_score = -1

    for employee in employees:
        score = 0

        if employee.skill == task.required_skill:
            score += 50
        else:
            score -= 20

        score -= employee.workload * 10

        if employee.state == "idle":
            score += 20
        elif employee.state == "working":
            score -= 10
        elif employee.state in ("problem", "burnout"):
            score -= 40

        score -= task.difficulty * 5
        score += max(0, task.deadline_time_left) // 10

        if score > best_score:
            best_score = score
            best_employee = employee

    return best_employee
```

### 2. Конечный автомат состояний сотрудника

**Зачем в проекте:** каждый сотрудник должен вести себя по-разному в зависимости от ситуации: ждать задачу, работать, столкнуться с проблемой или выгореть. Конечный автомат делает это поведение понятным и защищаемым.

**Как реализован в проекте:** логика находится в `entities/employee.py`. В игровом цикле `game.py` у каждого сотрудника вызывается метод `update(dt)`, который обновляет прогресс задачи и переводит сотрудника между состояниями.

**Входные данные:** текущее состояние сотрудника, назначенная задача, прошедшее время `dt`, нагрузка, мораль, наличие проблемы и решение игрока в диалоге.

**Выходные данные:** новое состояние сотрудника, прогресс задачи, изменение ресурсов проекта или создание кризисного события.

**Сложность:** `O(1)` для одного сотрудника за кадр. Для всей команды `O(n)`, где `n` - количество сотрудников. Память `O(1)` на сотрудника.

**Псевдокод:**

```python
def update_employee(employee, dt):
    if employee.state == "idle":
        return

    if employee.state == "working":
        employee.task_progress += employee.speed * dt
        employee.problem_risk += calculate_problem_risk(employee)

        if employee.problem_risk >= 100:
            employee.state = "problem"
            employee.problem_timer = 15
        elif employee.task_progress >= 100:
            complete_task(employee.task)
            employee.task = None
            employee.state = "idle"

    elif employee.state == "problem":
        employee.problem_timer -= dt
        if employee.problem_timer <= 0:
            apply_problem_penalty(employee)
            employee.state = "working"

    elif employee.state == "burnout":
        employee.speed = 0
```

### 3. Алгоритм генерации кризисных событий по уровню риска

**Зачем в проекте:** проблемы должны возникать не просто случайно, а логично: сложная задача, неподходящий исполнитель, перегруз и близкий дедлайн повышают шанс кризиса.

**Как реализован в проекте:** логика находится в `game.py` или `entities/employee.py`. Во время обновления сотрудника игра считает риск проблемы. Если случайное число меньше рассчитанного риска, создается событие: баг, выгорание, конфликт, пожар на сервере или ошибка ИИ-кода.

**Входные данные:** сложность задачи, соответствие навыка, нагрузка сотрудника, оставшееся время до дедлайна, мораль команды и текущий технический долг.

**Выходные данные:** новое кризисное событие или отсутствие события.

**Сложность:** `O(n)` за проверку всей команды, где `n` - количество сотрудников. Память `O(k)`, где `k` - количество активных событий. В худшем случае событие может появиться у каждого сотрудника.

**Псевдокод:**

```python
def calculate_problem_chance(employee, task, project_stats):
    chance = 5
    chance += task.difficulty * 4
    chance += employee.workload * 8
    chance += project_stats.tech_debt * 0.2

    if employee.skill != task.required_skill:
        chance += 25

    if task.deadline_time_left < 30:
        chance += 15

    if project_stats.morale < 40:
        chance += 10

    return min(chance, 80)


def try_create_problem(employee, project_stats):
    if employee.task is None:
        return None

    chance = calculate_problem_chance(employee, employee.task, project_stats)
    roll = random.randint(1, 100)

    if roll <= chance:
        return create_problem_event(employee)

    return None
```

### 4. Алгоритм проверки столкновений игрока с объектами офиса

**Зачем в проекте:** игрок не должен проходить сквозь стены, столы и другие объекты. Это базовая, но важная механика для top-down игры на pygame.

**Как реализован в проекте:** логика находится в `entities/player.py` и вызывается из игрового цикла `game.py`. Карта офиса задается прямоугольными препятствиями через `pygame.Rect`: стены, столы, доска задач, мебель. Сначала считается новое положение игрока, затем проверяется пересечение с препятствиями.

**Входные данные:** прямоугольник игрока, направление движения, скорость, `dt`, список прямоугольников-препятствий.

**Выходные данные:** новая позиция игрока без прохождения сквозь препятствия.

**Сложность:** `O(m)` за кадр, где `m` - количество препятствий. Память `O(1)`. В худшем случае проверяется столкновение со всеми объектами на карте.

**Псевдокод:**

```python
def move_player(player, dx, dy, obstacles):
    player.rect.x += dx
    for obstacle in obstacles:
        if player.rect.colliderect(obstacle):
            if dx > 0:
                player.rect.right = obstacle.left
            elif dx < 0:
                player.rect.left = obstacle.right

    player.rect.y += dy
    for obstacle in obstacles:
        if player.rect.colliderect(obstacle):
            if dy > 0:
                player.rect.bottom = obstacle.top
            elif dy < 0:
                player.rect.top = obstacle.bottom
```

### 5. Алгоритм подсчета финального счета

**Зачем в проекте:** в конце игры нужно оценить, насколько хорошо игрок справился с ролью проджект-менеджера. Счет дает понятную обратную связь: можно выиграть, но получить плохую оценку из-за техдолга, выгорания или низкого качества.

**Как реализован в проекте:** логика находится в `game.py` или отдельной функции подсчета результата. Алгоритм вызывается один раз при релизе продукта или при завершении игры.

**Входные данные:** количество выполненных задач, количество задач в срок, бюджет, мораль, качество, технический долг и доверие заказчика.

**Выходные данные:** итоговый счет и текстовая оценка результата.

**Сложность:** `O(1)` по времени и памяти, потому что используется фиксированное количество показателей.

**Псевдокод:**

```python
def calculate_score(stats):
    score = 0

    score += stats.completed_tasks * 100
    score += stats.tasks_done_in_time * 50
    score += stats.quality * 5
    score += stats.morale * 3
    score += stats.budget * 2
    score += stats.client_trust * 4
    score -= stats.tech_debt * 6

    return max(0, score)
```

## Поражение

Игрок проигрывает, если стартап разваливается до релиза: заканчивается бюджет, команда полностью выгорает, качество продукта падает до критического уровня или заказчик теряет доверие и отменяет проект.

## Примеры событий

- Девопс неправильно настроил сервер, и он буквально "загорелся" - нужно срочно тушить проблему.
- Заказчик меняет требования за день до дедлайна.
- В последний момент выясняется, что один из разработчиков писал код полностью с помощью ИИ, и теперь все ломается.
- Дизайнер отказывается править кнопку, потому что "она уже идеально синяя".
- QA нашел критический баг за пять минут до демо.
